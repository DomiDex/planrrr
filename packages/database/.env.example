# Database Connection
# PostgreSQL connection string for Prisma Client
DATABASE_URL="postgresql://user:password@localhost:5432/planrrr?schema=public"

# Direct database connection (for migrations)
# Use the same as DATABASE_URL for local development
# For serverless providers like Neon, use the pooled connection for DATABASE_URL
# and the direct connection for DIRECT_DATABASE_URL
DIRECT_DATABASE_URL="postgresql://user:password@localhost:5432/planrrr?schema=public"

# Optional: Shadow database for migrations (development only)
# SHADOW_DATABASE_URL="postgresql://user:password@localhost:5432/planrrr_shadow?schema=public"

# Node environment
NODE_ENV="development"